apiVersion: v1
kind: Service
metadata:
    name: "koencluster-637b"
    labels:
        app: "koencluster-637b"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 9000
      protocol: TCP
      name: http
    selector:
        app: "koencluster-637b"